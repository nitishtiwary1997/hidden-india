import React from 'react';
import HomeClient from '@/components/home/HomeClient';

export const metadata = {
  title: 'HiddenIndia.online — World-Class AI Powered Travel & Local Discovery Platform',
  description: 'Explore every state, district, city, village, temple, waterfall, and hidden place across India with AI trip planning and complete travel guides.',
};

export default function Home() {
  return <HomeClient />;
}
